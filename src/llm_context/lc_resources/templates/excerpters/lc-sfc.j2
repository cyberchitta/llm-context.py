## Content Excerpts - Key Sections

Excerpted content showing important sections from files. These sections are configurable per file type:

{% for item in sfc_excerpts %}
{%- set sections = item.metadata.sections_included or ["script"] -%}
- **{{ item.rel_path }}**: {{ sections | join(", ") | title }} sections
{%- if tools_available -%}
  {%- set excluded = [] -%}
  {%- if "style" not in sections -%}
    {%- set excluded = excluded + ["styles"] -%}
  {%- endif -%}
  {%- if "template" not in sections -%}
    {%- set excluded = excluded + ["templates"] -%}
  {%- endif -%}
  {%- if excluded -%}
 (use lc-get-excluded for {{ excluded | join(", ") }})
  {%- endif -%}
{%- endif %}
{% endfor %}

{% if tools_available %}
**Excluded Content Retrieval**
For content excluded from these excerpts, use lc-get-excluded tool.
{% else %}
**Excluded Content Retrieval**
To get excluded sections (styles, templates, etc.), ask the human to run `lc-get-excluded`
{% endif %}

{% for item in sfc_excerpts %}
{{ item.rel_path }}
॥๛॥
{{ item.content }}
॥๛॥
{% endfor %}